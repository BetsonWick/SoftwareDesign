OwnerId:
  in: path
  name: ownerId
  required: true
  schema:
    type: integer
    format: int64
    description:
      OwnerId

NDays:
  in: path
  name: nDays
  required: true
  schema:
    type: integer
    format: int64
    description:
      NDays

DateTo:
  in: query
  name: dateTo
  required: true
  schema:
    type: string
    format: date